# MBB PPT Generator — runtime dependencies.
# For development with optional features, see pyproject.toml extras:
#   pip install -e ".[dev]"        # core + pytest
#   pip install -e ".[image]"      # core + rembg/Pillow/numpy for image processing
#   pip install -e ".[all]"        # everything

python-pptx>=0.6.21
lxml>=4.9
pyyaml>=6.0
